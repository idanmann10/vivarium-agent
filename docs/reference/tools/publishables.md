---
title: Publishables Tool
description: Self-tool reference for queued publishable artifacts.
when_to_read: When queuing or listing anonymized artifacts ready for world contribution.
---

# Publishables Tool

The `publishables` self-tool stores anonymized artifacts that may later be proposed to a world.

- `queue(artifact)` records a run, trace, skill, or anti-pattern artifact body with its path.
- `list()` returns queued publishable artifacts.
