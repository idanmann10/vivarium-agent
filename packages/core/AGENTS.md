# Core Guidance

Do not add I/O here. Add new shapes under `src/types/`, formulas under `src/math/`, and kernel changes in `src/kernel.ts`.

Every math formula and branded ID factory needs unit tests.
