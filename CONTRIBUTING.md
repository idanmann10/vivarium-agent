# Contributing

Use conventional commits. Run `bun run lint && bun run typecheck && bun run test` before opening a PR.

Phase work must link to a Discussion or plan entry when it changes architecture, core types, math thresholds, or kernel behavior.
